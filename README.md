How to use: Setup guide

note for `Windows`: Use `git bash` terminal if you are on windows.

Copy and run:
```bash
iwr https://raw.githubusercontent.com/fersonull/rn-dependency-installer/main/setup.sh | bash
